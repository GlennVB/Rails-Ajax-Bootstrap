== RAILS, AJAX, TWITTER BOOTSTRAP seed project

A bootstrap project to get prototyping very fast.
Bootstrap is enabled by default. 
Scaffolding generates AJAX pages for faster frontend. 

== NEXT STEPS
* Add default user and api setup
* Capistrano default deploy configuration
